#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Generate CS-Xmind-Note (SSHeRun/CS-Xmind-Note) minimal mindmap notes for Modules 01~07.

Adheres strictly to the 408 CS examination mindmap style:
- Markdown headings (# -> ## -> ###)
- Pure multi-level list trees (* and 4-space indents)
- Single-line quotes (* > 核心定义: ...)
- Structured step/feature breakdowns
- Zero oral noise / high factual density
- Native 1-click import into XMind and Markmap
"""

from pathlib import Path

def generate_notes(workspace_dir: Path):
    notes_dir = workspace_dir / "notes"
    notes_dir.mkdir(parents=True, exist_ok=True)

    # -------------------------------------------------------------
    # 模块 01：数据存储与运算 (P09 ~ P21)
    # -------------------------------------------------------------
    mod01_content = """# 模块 01：数据存储与运算（涵盖 P09 - P21）

> 计算机底层信息建模与算术逻辑体系 | 风格：精简 (minimal) - CS-Xmind-Note 408考研思维导图树状笔记 | 涵盖分集：P09 ~ P21 | 核心议题：字面量与类型、变量与内存绑定、字符串处理、人机I/O与算术逻辑运算符

---

## 知识拓扑框架导图

```text
模块 01：数据存储与运算 (Data Storage & Arithmetic)
├── [1] 数据实体与字面量表征
│   ├── 字面量体系 (P09) ──────── 整数 / 浮点数 / 字符串 / 布尔值直接量
│   ├── 核心数据类型 (P13) ────── 内置 type() 探查与动态类型绑定机制
│   └── 字符串定义与转义 (P14) ── 单双引号嵌套原则与三引号多行规范
├── [2] 内存状态与变量管理
│   ├── 变量机制 (P10) ────────── 变量定义格式、内存引用绑定与命名规范
│   ├── 标识符与保留字 (P11) ──── PEP 8 命名规范与 35 个保留字保护
│   └── 变量置换机制 (P12) ────── 传统中间变量中转 vs 原生元组解包极速置换
├── [3] 字符串工程处理
│   ├── 字符串拼接 (P15) ──────── 加号连接限制与 str.join() 批量高性能合并
│   └── 字符串格式化 (P16) ────── % 占位符、format() 方法到 f-string 原生插值
├── [4] 标准人机 I/O 交互
│   └── 标准输入输出 (P17) ────── print() 参数定制 (sep/end) 与 input() 强类型转换
└── [5] 运算符运算体系
    ├── 算术运算符 (P18) ──────── 真除 (/) vs 整除 (//) 与负数取模 (%) 语义
    ├── 赋值运算符 (P19) ──────── 复合赋值 (+=, *=) 计算时序与内存就地操作
    ├── 比较运算符 (P20) ──────── 比较关系表达式、链式比较与 == 对比 is
    └── 逻辑运算符 (P21) ──────── and / or / not 优先级与短路求值防御引擎
```

---

## 1. 核心知识思维导图与考纲笔记

### 1.1 字面量体系 (P09)
* 字面量 (Literals)
    * > 核心定义：在代码中直接给出的固定值（不可变常量），词法分析期即被解释器封装为内置常量对象。
    * 常用内置字面量分类
        * 整数 (int): 如 10, -5, 0，在 Python 3 中支持任意精度无溢出上限
        * 浮点数 (float): 如 3.14, -0.01, 2e3，基于 IEEE 754 双精度标准实现
        * 字符串 (str): 单引号或双引号包裹的有序字符序列，Unicode 全字符集
        * 布尔值 (bool): True (真) 与 False (假)，本质是 int 的子类 (True==1, False==0)
    * 语法与输出范式
        * 代码范式: `print(666); print(13.14); print("黑马程序员"); print(True)`
        * 类型探查: `type(666) -> <class 'int'>`
    * 考纲避坑要点
        * 布尔字面量首字母必须严格大写，小写 `true` / `false` 会被当做未定义变量抛出 `NameError`
        * 浮点数计算遵循二进制浮点标准，存在精度误差（如 `0.1 + 0.2 != 0.3`）

### 1.2 变量与内存绑定机制 (P10)
* 变量 (Variable)
    * > 核心定义：程序运行用于存储和标记数据的内存引用名称，格式为 `变量名 = 变量值`。
    * 变量底层运行机理
        * 动态指针绑定: 变量本身没有数据类型，类型属于内存中的堆对象；`=` 是将变量名指向对象地址
        * 引用计数 (Reference Count): 对象被引用时计数自增，计数归零时被垃圾回收器 (GC) 自动销毁
    * 变量的核心特征
        * 可变性: 同一变量名可多次重新绑定到不同类型和值的新对象
        * 状态累加: 支持在原值基础上计算更新（如 `money = money - 10`）

### 1.3 标识符规范与保留字系统 (P11)
* 标识符 (Identifier)
    * > 核心定义：开发者在编写代码时为变量、函数、类、模块等自定义命名的字符序列。
    * 硬性语法规则（违背则触发 SyntaxError）
        * 组成元素: 只能包含英文大小写字母、数字、下划线 `_`
        * 数字限制: 数字严禁作为开头字符（如 `1_a` 非法）
        * 大小写敏感: `Name` 与 `name` 为完全独立的两个标识符
        * 保留字冲突: 严禁占用 Python 内置的 35 个关键字（如 `class`, `def`, `if`, `for` 等）
    * PEP 8 行业工程规范
        * 变量名/函数名: 蛇形下划线法（snake_case，如 `user_account_balance`）
        * 类名: 大驼峰法（PascalCase，如 `OrderManager`）
        * 常量名: 全大写加下划线（UPPER_CASE，如 `MAX_CONNECTIONS`）

### 1.4 变量状态置换与解包机制 (P12)
* 变量交换 (Value Swapping)
    * > 核心定义：将两个变量持有的对象引用进行对调的计算过程。
    * 传统模式 vs Python 原生解包模式
        * 传统中间变量法:
            * 步骤: 1. `temp = a` -> 2. `a = b` -> 3. `b = temp`
            * 劣势: 需额外开辟第三个指针存储空间，多步操作易出错
        * Python 原生解包法:
            * 语法: `a, b = b, a`
            * 机理: 右侧先在底层操作数栈打包为匿名元组 `(b, a)`，随后原子解包赋值给左侧变量，无需显式临时变量

### 1.5 核心数据类型体系与 type() 探查 (P13)
* 数据类型 (Data Types)
    * > 核心定义：Python 对不同形式数据的分类规范，决定了数据在内存中的存储结构与合法运算。
    * 常用核心内置类型
        * `int`: 有符号长整型，内部按动态数组存储多位大数
        * `float`: 双精度 64 位浮点数（C 语言 double 实现）
        * `str`: 不可变 Unicode 字符串序列
        * `bool`: 布尔类型，继承自 int
    * 类型探查工具: `type(目标表达式)`
        * 返回值: 类型对象（如 `<class 'str'>`）
        * 典型用例: `res_type = type("Hello")`，可直接打印或比对

### 1.6 字符串定义与转义规范 (P14)
* 字符串定义 (String Literal Definition)
    * > 核心定义：由成对引号包裹的字符序列，用于在程序中表达文本信息。
    * 四种定义形式
        * 单引号: `'Hello'`（适用于简短单行文本）
        * 双引号: `"Hello"`（适用于含单引号的单行文本）
        * 三单引号: `'''...'''`（支持跨多行保留换行格式，未绑定变量时常作多行注释）
        * 三双引号: `\"\"\"...\"\"\"`（标准多行文本与函数文档字符串 Docstring 规范）
    * 引号嵌套规则
        * 外双内单: `"I'm a student"`（无需转义）
        * 外单内双: `'He said: "Python is great!"'`（无需转义）
        * 反斜杠转义: `\"` 或 `\\'`，使用 `\\` 消除特殊字符语义

### 1.7 字符串拼接机理 (P15)
* 字符串拼接 (Concatenation)
    * > 核心定义：将两个或多个独立的字符串按照顺序合并为一个新字符串。
    * 加号 `+` 拼接规约
        * 严格同类型约束: 仅支持 `str + str`，字符串与数字使用 `+` 会抛出 `TypeError`
        * 内存机理: 由于字符串是不可变对象（Immutable），每次 `+` 都会在内存中开辟新空间并复制字符
    * 高性能批处理方案
        * `str.join(iterable)`: 在批量合并大量文本时，比连续 `+` 拥有线性的时间复杂度

### 1.8 字符串格式化三代演进 (P16)
* 字符串格式化 (String Formatting)
    * > 核心定义：按照预设的模板与数据类型要求，将变量动态插入文本指定位置的过程。
    * 三代技术路线对比
        * 第一代: 占位符 `%`
            * 格式: `"姓名: %s, 年龄: %d, 成绩: %.2f" % (name, age, score)`
            * 特点: 语法源自 C 语言 `printf`，支持精度控制 `%m.nf`（m 为总宽度，n 为小数位数）
        * 第二代: `format()` 方法
            * 格式: `"姓名: {}, 年龄: {}".format(name, age)`
            * 特点: 面向对象方法，支持位置索引与关键字传参
        * 第三代: `f-string` 插值 (PEP 498 - 强烈推荐)
            * 格式: `f"姓名: {name}, 资产: {money:.2f}"`
            * 特点: 运行期直接内联求值，支持任意 Python 表达式，性能与可读性达到最优

### 1.9 标准人机 I/O 交互 (P17)
* 标准输入与输出 (Standard I/O)
    * > 核心定义：程序与终端控制台之间进行数据展示与用户交互的通道。
    * `print()` 输出函数高级参数
        * `sep`: 多值之间的分隔符，默认为单个空格 `' '`（如 `print(1, 2, sep="---")`）
        * `end`: 输出结束符，默认为换行符 `\\n`（设为 `end=""` 可实现行内连续打印不换行）
    * `input()` 输入函数核心机制
        * 阻塞特性: 执行时程序完全挂起，等待用户键盘输入并按下回车
        * 强转约束: 无论用户输入何种内容，`input()` 返回值**恒为字符串 `str`**
        * 数值转换: 参与数值运算前必须使用 `int()` 或 `float()` 进行显式类型转换

### 1.10 算术运算符体系 (P18)
* 算术运算符 (Arithmetic Operators)
    * > 核心定义：执行基础数学四则与指数求模运算的操作符集合。
    * 常用符号与底层逻辑
        * 加减乘: `+`, `-`, `*`
        * 真除法 `/`: 计算结果**恒为浮点数 float**（如 `4 / 2 == 2.0`）
        * 整除 `//`: 向下取整截断（如 `7 // 2 == 3`, `-7 // 2 == -4`）
        * 取模 `%`: 求除法余数（满足公式: `a == (a // b) * b + (a % b)`）
        * 指数 `**`: 乘方幂运算（如 `2 ** 3 == 8`）
    * 优先级规则
        * `()` 括号最高 > `**` 乘方 > `*`, `/`, `//`, `%` > `+`, `-`

### 1.11 赋值与复合赋值运算符 (P19)
* 赋值运算符 (Assignment Operators)
    * > 核心定义：将右侧表达式的求值结果绑定到左侧变量名上的操作符。
    * 基础赋值: `=`（自右向左求值绑定）
    * 复合赋值运算符: `+=`, `-=`, `*=`, `/=`, `//=`, `%=`, `**=`
        * 展开等价性: `x += 1` 逻辑上等价于 `x = x + 1`
        * 计算时序: 先完成右侧整个表达式的完整计算，最后执行赋值
        * 就地操作优化: 对可变对象（如列表）复合赋值会尝试就地修改，减少内存开销

### 1.12 比较关系运算符与 == vs is (P20)
* 比较运算符 (Comparison Operators)
    * > 核心定义：用于比对两个对象之间序关系或等价关系的布尔判定操作符。
    * 常用运算符
        * `==`: 值相等判定（调用 `__eq__()`）
        * `!=`: 值不相等判定（调用 `__ne__()`）
        * `>`, `<`, `>=`, `<=`: 大小顺序关系比对
    * 核心辨析: `==` (数值相等) vs `is` (内存同一性)
        * `==`: 比对两个对象持有的内容/数值是否相同
        * `is`: 比对两个变量是否指向同一个底层内存地址（`id(a) == id(b)`）
        * 考纲规约: 与单例对象（如 `None`, `True`, `False`）比对时必须使用 `is`（如 `if x is None:`）

### 1.13 布尔逻辑运算符与短路求值 (P21)
* 逻辑运算符 (Logical Operators)
    * > 核心定义：对布尔表达式进行与、或、非布尔代数运算的连接符。
    * 符号与逻辑规则
        * `and` (逻辑与): 仅当两端均为真时结果为真（一假即假）
        * `or` (逻辑或): 只要任一端为真结果即为真（一真即真）
        * `not` (逻辑非): 单目运算符，取反布尔真值
    * 运算符优先级: `not` (最高) > `and` > `or` (最低)
    * 短路求值引擎 (Short-Circuit Evaluation)
        * `and` 短路: 从左到右扫描，若遇到首个为假的表达式，直接停止后续求值并返回该操作数
        * `or` 短路: 从左到右扫描，若遇到首个为真的表达式，直接停止后续求值并返回该操作数
        * 防御性编程: 利用短路保护空指针/越界访问（如 `if obj is not None and obj.value > 0:`）

---

## 2. 关键语法与常用运算符矩阵

| 语法分类 | 关键符号 / 函数 | 典型代码范式 | 核心行为 / 规约 |
| :--- | :--- | :--- | :--- |
| **基础类型** | `int`, `float`, `str`, `bool` | `type(x)` | 动态类型推断，底层基于 PyObject 封装 |
| **命名空间** | `=` | `a, b = b, a` | 原生元组解包交换，底层借助操作数栈原地置换 |
| **文本渲染** | `f"{var:.2f}"` | `f"User: {name}, Cash: {money:.2f}"` | PEP 498 原生格式化插值，具备极致的运行时解析效率 |
| **终端交互** | `print()`, `input()` | `age = int(input("Age:"))` | 标准 I/O 阻塞流，input 结果必为 str，需显式强转 |
| **算术除除** | `/`, `//`, `%` | `7 / 2 == 3.5`, `7 // 2 == 3` | 真除法产生 float，整除法向下取整，取模满足数学余数定义 |
| **逻辑短路** | `and`, `or`, `not` | `val = custom or default_val` | 逻辑判定短路执行，直接返回决定最终真假的操作数本身 |

---

## 3. 要点对比横评矩阵

### 对比一：`==` (数值比对) vs `is` (内存同一性比对)
| 判定维度 | `==` (Equality) | `is` (Identity) |
| :--- | :--- | :--- |
| **底层原理** | 调用对象的 `__eq__()` 魔法方法 | 直接比对变量底层内存指针地址（`id(a) == id(b)`） |
| **比对目标** | 对象的内容、数值、数据状态 | 变量是否指向同一块物理内存单元 |
| **典型案例** | `[1, 2] == [1, 2]` 结果为 `True` | `[1, 2] is [1, 2]` 结果为 `False`（独立堆对象） |
| **适用规约** | 绝大多数业务数据比对 | 单例对象比对（`x is None`, `flag is True`） |

### 对比二：整除 `//` vs 取模 `%`
| 运算符号 | 运算名称 | 计算数学定义 | 负数边界案例 |
| :--- | :--- | :--- | :--- |
| `//` | 整除截断 | 向数轴负无穷方向向下取整 | `7 // 2 == 3`, `-7 // 2 == -4` |
| `%` | 取模求余 | `r = a - (a // b) * b` | `7 % 2 == 1`, `-7 % 2 == 1`（余数符号与除数一致） |
"""
    (notes_dir / "模块01_数据存储与运算_复习笔记.md").write_text(mod01_content.strip() + "\n", encoding="utf-8")
    print("[✓] 已生成 CS-Xmind-Note 风格笔记: 模块01_数据存储与运算_复习笔记.md")

    # -------------------------------------------------------------
    # 模块 02：流程控制语句 (P22 ~ P35)
    # -------------------------------------------------------------
    mod02_content = """# 模块 02：流程控制语句（涵盖 P22 - P35）

> 程序图灵完备性核心：分支决策与循环迭代引擎 | 风格：精简 (minimal) - CS-Xmind-Note 408考研思维导图树状笔记 | 涵盖分集：P22 ~ P35 | 核心议题：布尔逻辑、if多向条件分支、while条件循环、for可迭代对象遍历、break/continue控制跳转

---

## 知识拓扑框架导图

```text
模块 02：流程控制语句 (Control Flow & Loops)
├── [1] 分支条件判断体系
│   ├── 布尔与比较基础 (P22) ──── True/False 状态与条件测试
│   ├── if 基础与缩进规则 (P23-24) 4 空格缩进块作用域与执行逻辑
│   ├── if-else 双向互斥 (P25) ── 二选一分支流
│   ├── if-elif-else 多向分支 (P26) 多条件互斥流与自上而下匹配
│   ├── if 嵌套逻辑 (P27) ────── 树状深层条件决策
│   └── 分支综合案例 (P28) ────── 猜数字小游戏业务闭环
└── [2] 循环迭代控制引擎
    ├── while 条件循环体系 (P29) ─ 循环前置条件、步进变量与死循环防御
    ├── while 累加实战 (P30) ─── 1-100 累加计数状态机
    ├── while 循环嵌套 (P31) ─── 二维矩阵遍历与九九乘法表排版
    ├── for 容器遍历引擎 (P32) ── 遍历可迭代对象（序列逐项提取）
    ├── range() 序列生成器 (P33) ─ 左闭右开区间与步长控制 (start:stop:step)
    ├── 跳转中断关键字 (P34) ──── break 强行终止整轮 vs continue 跳过当次
    └── 循环综合案例 (P35) ────── 员工发工资模拟与状态流转
```

---

## 1. 核心知识思维导图与考纲笔记

### 1.1 布尔类型与条件测试基础 (P22)
* 布尔逻辑 (Boolean Logic)
    * > 核心定义：计算机判断真伪的二值逻辑体系，包含字面量 `True` 与 `False`。
    * 对象的真假性判断（Truthy & Falsy）
        * 隐式为 False 的对象: `0`, `0.0`, `""` (空串), `[]` (空列表), `()` (空元组), `{}` (空字典), `set()` (空集合), `None`
        * 其余所有非空、非零对象在逻辑判定中均隐式为 `True`

### 1.2 if 条件分支基础与 PEP 8 缩进语法块 (P23 - P24)
* if 条件判断 (Conditional Branching)
    * > 核心定义：根据条件表达式的真假状态，决定是否执行特定代码块的基本控制结构。
    * 语法格式
        * 结构: `if 条件表达式:\n    代码块...`
    * Python 缩进作用域机理
        * 无大括号: Python 放弃 C/Java 的 `{}`，强制以**4 个空格缩进**界定代码块归属
        * 冒号标志: `if 条件:` 末尾的冒号是开启子作用域代码块的语法标志，遗漏将触发 `SyntaxError`
        * 执行路径: 条件判定为 `True` 时执行缩进代码块；为 `False` 时直接跳过代码块继续向下执行

### 1.3 if-else 双向互斥分支 (P25)
* if-else 分支
    * > 核心定义：提供“非此即彼”的双向互斥路径，满足条件走分支 A，否则走分支 B。
    * 语法与规范
        * 结构: `if 条件:\n    分支A\nelse:\n    分支B`
        * 互斥性: 分支 A 与 分支 B 有且仅有一个会被执行
        * 对齐规则: `else:` 与对应的 `if:` 必须保持相同列的缩进对齐

### 1.4 if-elif-else 多向互斥分支 (P26)
* 多条件分支 (Chained Conditionals)
    * > 核心定义：按照优先次序检查多个互斥条件，执行首个命中的逻辑分支。
    * 匹配与短路执行流程
        * 自上而下: 解释器自上而下依次求值条件表达式
        * 命中即止: 一旦某个 `elif 条件` 为 `True`，执行其对应代码块后**直接跳出整个 if 结构**，后续条件不再求值
        * 兜底保护: `else:` 为可选兜底，仅当前面所有条件均不满足时执行

### 1.5 if 嵌套结构与树状决策 (P27 - P28)
* 条件嵌套 (Nested Conditionals)
    * > 核心定义：在一个条件代码块内部完整嵌入另一层条件分支结构，用于多级前置验证场景。
    * 缩进层级管理
        * 外层缩进 4 空格，内层缩进 8 空格，层级越深缩进越多
        * 工程规约: 嵌套层级原则上**不超过 3 层**，过深嵌套应通过提前 `return`（卫语句 Guard Clauses）或重构逻辑重平铺

### 1.6 while 循环基础与步进状态机 (P29 - P30)
* while 循环 (Condition-based Loop)
    * > 核心定义：只要给定的循环控制条件为 `True`，便持续重复执行循环体代码块。
    * 循环三要素（缺一不可，否则导致死循环 Infinite Loop）
        * 1. 循环初始条件: 循环变量初始化（如 `i = 0`）
        * 2. 循环判断条件: 循环继续的边界（如 `while i < 100:`）
        * 3. 循环变量更新 (步进): 推动循环趋向终止的变量累加（如 `i += 1`）
    * 典型范式: 1-100 累加计数
        * 状态变量初始化 `sum_val = 0; i = 1`
        * 循环体内部执行累加 `sum_val += i` 与步进 `i += 1`

### 1.7 while 循环嵌套与二维排版 (P31)
* 嵌套循环 (Nested Loops)
    * > 核心定义：外层循环每执行一次，内层循环完整执行一个全周期的复合控制结构。
    * 二维控制矩阵
        * 外层循环控制“行”（Row），内层循环控制“列”（Column）
    * 九九乘法表排版技术
        * 动态列边界: 内层循环条件由外层行号动态决定（`j <= i`）
        * 行内连续打印: `print(..., end="")` 阻止自动换行
        * 列对齐排版: 使用制表符 `\t` 进行等宽制表对齐
        * 行末换行: 内层循环结束后调用空 `print()` 换行

### 1.8 for 循环基础与可迭代序列遍历 (P32)
* for 循环 (Iterator-based Loop)
    * > 核心定义：用于逐项遍历可迭代对象（如字符串、列表、元组、range）中元素的遍历引擎。
    * 语法格式
        * `for 临时变量 in 可迭代对象:`
    * 运行机理:
        * 轮询提取: 每轮循环自动从容器中提取下一个元素绑定给临时变量，直到容器耗尽自动结束
        * 边界安全: 无需显式维护下标与自增计数器，从根本上杜绝数组越界异常

### 1.9 range() 序列生成器体系 (P33)
* `range` 内置类型
    * > 核心定义：生成一系列连续或等步长整数的不可变数字序列生成器（惰性求值求序列）。
    * 三种构造形态
        * `range(stop)`: 默认起始于 0，步长为 1，区间为 `[0, stop)`（左闭右开，不含 stop）
        * `range(start, stop)`: 指定起始值 start，区间为 `[start, stop)`
        * `range(start, stop, step)`: 指定起始值、结束值与步长 step
    * 步长边界规则
        * 正步长: start 必须小于 stop，递增序列（如 `range(1, 10, 2)` 生成 1, 3, 5, 7, 9）
        * 负步长: start 必须大于 stop，递减序列（如 `range(10, 0, -1)` 生成 10, 9, ..., 1）
        * 步长为 0: 触发 `ValueError: range() arg 3 must not be zero`

### 1.10 break 与 continue 跳转中断控制 (P34 - P35)
* 循环跳转关键字
    * > 核心定义：在循环正常执行周期中，强制变更执行流程的跳转语句。
    * `continue` (跳过本次循环)
        * 核心语义: 立即终止当前这次迭代，跳过循环体剩余代码，直接进入下一次循环判断
        * 应用场景: 遇到无效/非法数据时提前跳过，继续处理后续项
    * `break` (彻底终止整轮循环)
        * 核心语义: 立即强行终止当前所在的最内层循环，直接跳出循环体执行外部后续代码
        * 应用场景: 搜索算法命中目标数据后即刻停机，避免无效遍历
    * 嵌套循环作用域约束
        * `break` 和 `continue` **仅对当前所在的最直接内层循环生效**，绝不会直接击穿跳出外层循环

---

## 2. 核心控制语句语法矩阵

| 语句类型 | 典型语法结构 | 执行条件与语义 | 典型应用场景 |
| :--- | :--- | :--- | :--- |
| **if 单向** | `if cond:` | 仅当 cond 为真执行 | 前置数据校验、简单开关触发 |
| **if-else 双向** | `if cond: ... else: ...` | 二选一互斥执行 | 权限判断、状态二值分支 |
| **if-elif 多向** | `if c1: ... elif c2: ... else: ...` | 自上而下命中即止 | 分数等级划分、状态码分流 |
| **while 循环** | `while cond: step` | 满足条件持续迭代 | 不确定循环次数的任务、服务主循环 |
| **for 循环** | `for x in iterable:` | 遍历序列容器所有元素 | 已知集合遍历、批量列表批处理 |
| **continue** | `if filter_cond: continue` | 跳过当次，进入下次循环 | 数据清洗中过滤空行与脏数据 |
| **break** | `if match_cond: break` | 立即彻底跳出当前循环 | 线性查找命中立刻返回、死循环退出 |

---

## 3. 核心机制对比横评

### 对比一：`while` 循环 vs `for` 循环
| 维度 | `while` 循环 | `for` 循环 |
| :--- | :--- | :--- |
| **驱动机制** | 条件表达式驱动（基于 True/False） | 可迭代对象驱动（基于容器元素耗尽） |
| **循环次数** | 既可确定，也可不确定（由动态运行时条件决定） | 严格确定（由可迭代容器元素个数决定） |
| **步进管理** | 必须手动在循环体内自增/更新变量 | 解释器自动迭代，无需手动维护变量 |
| **死循环风险** | 遗漏自增步进语句将立即引发死循环 | 遍历有限容器天然安全，极少死循环 |
| **首选场景** | 监听网络事件、等待用户输入、游戏渲染主循环 | 遍历列表、字符串解析、已知范围序列计算 |

### 对比二：`continue` vs `break`
| 跳转指令 | 中断范围 | 对外层循环影响 | 典型代码位置 |
| :--- | :--- | :--- | :--- |
| `continue` | 仅终止**当前轮次**的后续代码 | 无影响，继续本层循环后续轮次 | 循环体头部作为前置过滤器 |
| `break` | 彻底终止**当前整层**循环 | 无影响，仅结束内层循环并返回外层 | 命中关键结果或异常熔断点 |
"""
    (notes_dir / "模块02_流程控制语句_复习笔记.md").write_text(mod02_content.strip() + "\n", encoding="utf-8")
    print("[✓] 已生成 CS-Xmind-Note 风格笔记: 模块02_流程控制语句_复习笔记.md")

    # -------------------------------------------------------------
    # 模块 03：数据容器 (P36 ~ P56)
    # -------------------------------------------------------------
    mod03_content = """# 模块 03：数据容器（涵盖 P36 - P56）

> 内存复合数据组织与抽象：序列、集合与哈希映射 | 风格：精简 (minimal) - CS-Xmind-Note 408考研思维导图树状笔记 | 涵盖分集：P36 ~ P56 | 核心议题：list、tuple、str、set、dict 五大内置容器体系、切片语法、增删改查方法与多维选型矩阵

---

## 知识拓扑框架导图

```text
模块 03：数据容器 (Data Containers & Collections)
├── [1] 数据容器全局分类与特征 (P36)
│   └── 有序/无序、可变/不可变、重复/去重 三维正交坐标系
├── [2] 列表体系 (list) (P37 - P42)
│   ├── 下标索引机制 (P37) ────── 零基正向与负向倒序寻址
│   ├── 追加插入方法 (P38) ────── append() / extend() / insert() 扩容机理
│   ├── 元素删除方法 (P39) ────── del / pop() / remove() / clear()
│   ├── 查询统计方法 (P40) ────── index() / count() / len()
│   └── 容器遍历与实战 (P41-P42)  while 下标遍历 vs for 直接遍历
├── [3] 元组体系 (tuple) (P43 - P45)
│   ├── 只读定义与逗号陷阱 (P43) ─ 单元素元组 (x,) 规范与防篡改约束
│   ├── 只读查询方法 (P44) ────── index() / count() / len()
│   └── 嵌套可变对象特例 (P45) ── 元组内部嵌列表的局部可变性分析
├── [4] 字符串容器与切片 (str) (P46 - P48)
│   ├── 字符只读序列特性 (P46) ── 不可变字符数组与下标探查
│   ├── 序列通用切片语法 (P47) ── [start:end:step] 步长与方向控制
│   └── 常用文本方法 (P48) ────── replace() / split() / strip()
├── [5] 集合体系 (set) (P49 - P51)
│   ├── 散列去重与无序性 (P49) ── 底层哈希表、去重与无序索引失效
│   ├── 集合增删操作 (P50) ────── add() / remove() / pop() / clear()
│   └── 集合代数运算 (P51) ────── 并集 union / 差集 difference / 交集
├── [6] 字典体系 (dict) (P52 - P54)
│   ├── 哈希映射与键约束 (P52) ── 键值对映射、Key 唯一性与 Hashable 约束
│   ├── 字典增删改查方法 (P53) ── 键赋值 / pop() / keys() / values() / items()
│   └── 嵌套字典与循环 (P54) ──── 层次化数据结构与结构化解包
└── [7] 容器通用操作与多维对比 (P55 - P56)
    ├── 内置多态函数 ─────────── len() / max() / min() / sorted()
    ├── 容器类型强制转换 ──────── list() / tuple() / str() / set()
    └── 五大容器多维横评 ──────── 选型决策树与性能考量
```

---

## 1. 核心知识思维导图与考纲笔记

### 1.1 数据容器分类与三维特征空间 (P36)
* 数据容器 (Data Containers)
    * > 核心定义：用于在单个变量中组织、批量容纳多个数据元素的复合数据类型。
    * 三维分类判定体系
        * 1. 元素有序性: 元素是否具备固定物理顺序（支持正向/反向索引）
        * 2. 元素唯一性: 是否允许容纳重复值（支持重复 vs 自动去重）
        * 3. 内存可变性: 容器初始化后，其内部元素是否支持就地修改与动态增删

### 1.2 列表 (list) 结构与增删改查 (P37 - P42)
* 列表 (list)
    * > 核心定义：一种有序、可变、允许重复的通用动态数组容器，使用中括号 `[...]` 定义。
    * 索引机制
        * 零基正向索引: 首元素为 `list[0]`，末尾元素为 `list[n-1]`
        * 负向逆序索引: 末尾元素为 `list[-1]`，首元素为 `list[-n]`
    * 元素增添方法
        * `list.append(element)`: 在列表末尾追加单个元素，时间复杂度 O(1)
        * `list.extend(iterable)`: 将另一个可迭代容器中的所有元素逐项拆散追加至末尾
        * `list.insert(index, element)`: 在指定下标前插入元素，涉及后续元素物理后移，时间复杂度 O(n)
    * 元素删除方法
        * `del list[index]`: Python 关键字，直接按索引物理删除元素
        * `list.pop([index])`: 弹出指定索引元素并返回其值；省略参数默认弹出末尾元素
        * `list.remove(value)`: 查找并删除首个值匹配项；若元素不存在则抛出 `ValueError`
        * `list.clear()`: 彻底清空列表，重置为空列表 `[]`
    * 元素查询与统计
        * `list.index(value)`: 返回指定元素在列表中首次出现的索引下标；不存在报 ValueError
        * `list.count(value)`: 统计指定元素在列表中出现的总频次
        * `len(list)`: 返回列表当前包含的元素总个数

### 1.3 元组 (tuple) 只读特性与单元素陷阱 (P43 - P45)
* 元组 (tuple)
    * > 核心定义：一种有序、不可变（只读）、允许重复的序列容器，使用小括号 `(...)` 定义。
    * 核心价值: 安全防篡改、多返回值打包、可作为字典的 Key（因其 Hashable）。
    * 单元素元组逗号陷阱
        * 错误写法: `t = ("hello")` -> 会被 Python 解释器识别为带括号的普通常量字符串 `str`
        * 正确规范: `t = ("hello",)` -> **必须紧随一个逗号**，解释器方可判定为元组类型
    * 嵌套可变对象的特例
        * 规则: 元组本身的引用指针不可篡改，但若元组内部包含可变容器（如列表），该列表内部的元素允许就地修改
        * 示例: `t = (1, [2, 3]); t[1].append(4)` 合法，因为元组持有的列表地址未变

### 1.4 字符串 (str) 容器特性与序列切片 (P46 - P48)
* 字符串容器与切片
    * > 核心定义：字符串是字符的不可变有序序列，天然继承序列的一切只读操作与切片能力。
    * 序列通用切片语法 (Slice)
        * 语法格式: `序列[start:stop:step]`
        * 范围界定: 左闭右开区间 `[start, stop)`，不包含 stop 本身
        * 缺省缺省: 省略 start 默认从头开始；省略 stop 默认截取到末尾；省略 step 默认步长为 1
        * 逆序翻转范式: `序列[::-1]`，步长为 -1，从右向左全量反转提取
    * 核心常用方法
        * `str.replace(old, new, [count])`: 文本替换，返回新字符串（原字符串不变）
        * `str.split(sep)`: 按照指定分隔符拆分字符串，返回元素为子字符串的列表 `list`
        * `str.strip([chars])`: 剔除字符串首尾两端的空白符（制表符、换行符、空格）或指定字符集

### 1.5 集合 (set) 散列去重与集合代数运算 (P49 - P51)
* 集合 (set)
    * > 核心定义：一种无序、去重、元素不可变的哈希容器，使用花括号 `{...}` 或 `set()` 定义。
    * 核心特征
        * 自动去重: 任何重复添加的元素会被底层哈希表自动剔除，只保留一份
        * 无序存储: 元素在内存中按哈希散列分布，**完全不支持下标索引访问**（`s[0]` 抛 TypeError）
    * 增删操作
        * `s.add(val)`: 添加单元素
        * `s.remove(val)`: 移除指定元素，不存在则抛出 KeyError
        * `s.pop()`: 随机弹出一个元素并返回其值（因无序性，弹出顺序不可控）
    * 集合代数运算
        * 差集: `s1.difference(s2)` 返回 s1 独有而 s2 没有的元素，生成新集合
        * 就地差集: `s1.difference_update(s2)` 直接修改 s1，抹去与 s2 相同的交集元素
        * 并集: `s1.union(s2)` 返回包含 s1 与 s2 所有不重复元素的新集合
        * 交集: `s1.intersection(s2)` 返回两个集合共有的元素

### 1.6 字典 (dict) 键值对映射与哈希约束 (P52 - P54)
* 字典 (dict)
    * > 核心定义：基于哈希表实现的键值对（Key-Value）映射容器，格式为 `{key: value}`。
    * 键（Key）的硬性约束
        * 唯一性: Key 绝对不允许重复，重复赋值后新 Value 会覆盖旧 Value
        * 可哈希约束 (Hashable): Key **必须为不可变对象**（如 `int`, `float`, `str`, `tuple`），严禁使用 `list`, `dict`, `set` 作 Key
    * 常用操作方法
        * 访问修改: `d[key] = new_val`（Key 存在则修改，不存在则新增键值对）
        * 安全获取: `d.get(key, default)`（Key 不存在返回 default，不会抛出 KeyError）
        * 键删除: `d.pop(key)` 弹出并返回指定 Key 对应的 Value
        * 视图提取: `d.keys()`, `d.values()`, `d.items()`（返回键集合、值集合、键值对元组集合）
    * 字典循环遍历范式
        * 直接遍历键: `for k in d:`
        * 元组解包遍历: `for k, v in d.items():`

### 1.7 容器通用操作与多维对比选型 (P55 - P56)
* 通用内置多态函数
    * `len(c)`: 返回容器元素个数
    * `max(c)` / `min(c)`: 求解容器中元素的最大值与最小值（要求元素可比）
    * `sorted(c, reverse=False)`: 对任意容器元素排序，**统一返回新列表 `list`**
* 容器类型互转
    * `list(c)`: 转列表；`tuple(c)`: 转元组；`set(c)`: 极速去重；`str(c)`: 序列化为文本字面量

---

## 2. 五大数据容器特征全景矩阵

| 容器类型 | 标识符号 | 有序性 | 元素可重复 | 内存可变性 | 索引访问 | 典型底层结构 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **列表 (list)** | `[...]` | 有序 | 是 | 可变 | 零基 / 负向索引 | 动态指针数组 |
| **元组 (tuple)** | `(...)` | 有序 | 是 | **不可变** | 零基 / 负向索引 | 定长只读数组 |
| **字符串 (str)** | `""` / `''` | 有序 | 是 | **不可变** | 零基 / 负向索引 | 连续紧凑字符数组 |
| **集合 (set)** | `{...}` | **无序** | **否 (自动去重)** | 可变 | **不支持** | 散列哈希表 (HashTable) |
| **字典 (dict)** | `{k: v}` | 插入有序(3.7+) | Key否 / Value是 | 可变 | 基于 Key 索引 | 分离表头的高性能哈希表 |

---

## 3. 业务选型决策与易错避坑

### 选型决策树
1. 需要键值对映射？ -> **字典 (dict)**
2. 需要自动去重或进行交并差数学运算？ -> **集合 (set)**
3. 数据仅供只读保护、防篡改、作函数多返回值？ -> **元组 (tuple)**
4. 常规数据批量存储、需要按索引频繁修改追加？ -> **列表 (list)**

### 易错避坑要点
- `s = {}` 定义的是**空字典**而非空集合；定义空集合必须显式使用 `s = set()`。
- 切片操作不会越界：切片下标超出范围时会自动截断至两端，不会抛出 `IndexError`，而单索引访问超出范围必报 `IndexError`。
- 字典和集合在遍历过程中**严禁就地增删元素**，否则会破坏底层哈希迭代器抛出 `RuntimeError: dictionary changed size during iteration`。
"""
    (notes_dir / "模块03_数据容器_复习笔记.md").write_text(mod03_content.strip() + "\n", encoding="utf-8")
    print("[✓] 已生成 CS-Xmind-Note 风格笔记: 模块03_数据容器_复习笔记.md")

    # -------------------------------------------------------------
    # 模块 04：函数基础与进阶 (P57 ~ P70)
    # -------------------------------------------------------------
    mod04_content = """# 模块 04：函数基础与进阶（涵盖 P57 - P70）

> 软件模块化与控制抽象：调用栈帧、作用域链、不定长解包与一等公民 | 风格：精简 (minimal) - CS-Xmind-Note 408考研思维导图树状笔记 | 涵盖分集：P57 ~ P70 | 核心议题：def函数抽象、形参实参绑定、None底层语义、global作用域穿透、多返回值解包、缺省参数、*args/**kwargs、高阶函数与lambda

---

## 知识拓扑框架导图

```text
模块 04：函数基础与进阶 (Functions & Modular Abstraction)
├── [1] 函数定义与执行调用栈 (P57 - P62)
│   ├── 模块化封装价值 (P57) ──── 代码复用、逻辑解耦与单一职责原则
│   ├── def 声明语法 (P58-P59) ── 函数签名、形参 (Parameter) 与实参 (Argument)
│   ├── 返回值与 None 语义 (P60)  return 中断流与 NoneType 单例伪布尔
│   ├── Docstring 文档规约 (P61) 规范函数注释与 IDE 悬停提示
│   └── 嵌套调用与栈帧 (P62) ─── 调用栈帧 (Stack Frame) 入栈出栈时序
├── [2] 变量作用域与命名空间 (P63 - P64)
│   ├── 局部变量与全局变量 (P63)  LEGB 作用域解析规则与生命周期
│   └── global 关键字声明 (P64) ─ 显式穿透作用域绑定全局内存指针
└── [3] 函数高级参数与函数式编程 (P65 - P70)
    ├── 多返回值解包 (P65) ────── 元组隐式打包与多变量原子解包
    ├── 位置参数 vs 关键字参数 (P66) 按序绑定与键名指定传参
    ├── 默认参数机制 (P67) ────── 缺省参数靠右放置与可变对象默认值陷阱
    ├── 不定长参数体系 (P68) ──── *args 元组打包 vs **kwargs 字典打包
    ├── 函数作为一等公民 (P69) ── 函数名作参数传递与高阶函数设计
    └── lambda 匿名函数 (P70) ─── 单行内联计算与递归调用基础
```

---

## 1. 核心知识思维导图与考纲笔记

### 1.1 函数定义语法与形参实参绑定 (P57 - P59)
* 函数 (Function)
    * > 核心定义：组织好的、可重复使用的、用来实现单一或相关联功能的代码段。
    * 定义语法
        * `def 函数名([形参1, 形参2, ...]):\n    \"\"\"Docstring 注释\"\"\"\n    函数体\n    return [返回值]`
    * 形参与实参辨析
        * 形参 (Parameters): 函数定义签名中的变量占位符，仅在函数被调用时分配临时内存
        * 实参 (Arguments): 函数实际调用时传入的真实数据对象或变量引用

### 1.2 返回值机制与 None 底层语义 (P60)
* 返回值与 None
    * > 核心定义：`return` 语句用于将函数的计算结果返回给调用者，并立即中断当前函数的执行。
    * None 的底层机理
        * 类型归属: Python 内置的 `NoneType` 类型的唯一单例对象
        * 触发场景: 函数省略 `return`，或者显式写 `return` / `return None`
        * 逻辑真伪: 在 `if` 条件判定中被隐式视为 `False`
        * 典型用例: 变量初始化占位、函数执行无意义输出、搜索未命中的默认返回

### 1.3 函数文档注释 Docstring 规范 (P61)
* 文档字符串 (Docstring)
    * > 核心定义：紧随 `def` 声明的第一行使用三引号包裹的文本，用于解释函数功能、参数含义与返回值。
    * 规范结构:
        * 简要说明函数核心业务目的
        * `:param name: 参数类型与业务含义解释`
        * `:return: 返回值类型与结构解释`
    * 工具价值: 支持 IDE 悬停自动弹出 API 文档，可通过 `help(func)` 或 `func.__doc__` 在线读取

### 1.4 函数嵌套调用与调用栈帧 (P62)
* 调用栈与嵌套调用
    * > 核心定义：一个函数在其函数体内部直接或间接调用另一个函数的运行机制。
    * 栈帧运行模型 (Call Stack Frame)
        * 调用入口: 调用函数 A 时，A 的栈帧入栈（压入调用栈），分配局部命名空间
        * 嵌套触发: A 内部调用函数 B 时，A 的执行挂起，B 的栈帧压入栈顶
        * 执行完毕: B 执行遇到 return 时，B 的栈帧出栈销毁，释放其局部变量，控制权交还 A

### 1.5 变量作用域与 global 关键字 (P63 - P64)
* 作用域与命名空间 (Scope & Namespace)
    * > 核心定义：变量名称对程序的可见有效范围，遵循 LEGB 查找次序（Local -> Enclosing -> Global -> Built-in）。
    * 局部变量 (Local) vs 全局变量 (Global)
        * 局部变量: 定义在函数内部的变量，仅函数内可见，随栈帧销毁而释放
        * 全局变量: 定义在模块顶层的变量，在当前文件所有函数内均可读取
    * `global` 关键字声明规约
        * 读写差异: 函数内部可以直接**读取**全局变量；但直接对其执行 `=` 赋值操作会被解释器视为在当前局部创建同名局部变量
        * 强制声明: 若必须在函数内部修改全局变量的引用，必须在赋值前显式声明 `global 变量名`

### 1.6 多返回值解包与元组打包机制 (P65)
* 多返回值 (Multiple Returns)
    * > 核心定义：Python 函数语法支持一行代码返回多个数据对象。
    * 底层运行机理
        * 隐式元组打包: `return a, b, c` 在底层被自动封装为一个元组 `(a, b, c)`
        * 解包接收: 调用端使用逗号分隔的多个变量接收 `x, y, z = func()`，遵循元组解包规则，变量个数必须与返回值个数严格一致

### 1.7 传参方式与缺省参数陷阱 (P66 - P67)
* 函数传参分类
    * 位置参数 (Positional): 严格按照形参声明的从左到右位置顺序一一对应传入
    * 关键字参数 (Keyword): 通过 `形参名=值` 显式指定对应关系，可打乱传入次序
    * 缺省参数 (Default Arguments)
        * 声明规范: 带有默认值的形参**必须严格靠右声明**，否则报 `SyntaxError: non-default argument follows default argument`
        * 经典陷阱: **严禁使用可变对象（如列表、字典）作为默认值**（默认参数在函数定义期即被创建为共享对象，多次调用会导致状态污染；正确做法是默认值设为 `None`，函数内动态判断初始化）

### 1.8 不定长参数体系 (*args 与 **kwargs) (P68)
* 不定长参数 (Arbitrary Arguments)
    * > 核心定义：当调用者传入的实参个数不确定时，用于弹性容纳任意数量实参的机制。
    * `*args` (位置不定长)
        * 符号机制: 单星号将所有多余的未命名位置参数打包为一个**元组 (tuple)**
        * 形参命名约定: 惯用 `*args`（arguments）
    * `**kwargs` (关键字不定长)
        * 符号机制: 双星号将所有多余的关键字参数打包为一个**字典 (dict)**
        * 形参命名约定: 惯用 `**kwargs`（keyword arguments）
    * 复合声明顺序: `def func(标准位置, *args, 默认参数, **kwargs):`

### 1.9 函数作为一等公民与高阶函数 (P69)
* 一等对象 (First-Class Object)
    * > 核心定义：在 Python 中，函数被视为普通对象，可赋值给变量、可作为实参传入其他函数、可作为其他函数的返回值。
    * 高阶函数 (Higher-Order Functions)
        * 模式: 接收一个函数作为计算逻辑传入（如 `def compute(x, y, logic_func): return logic_func(x, y)`）
        * 架构解耦: 将“计算流程控制”与“具体计算策略”彻底解耦

### 1.10 lambda 匿名函数与递归基础 (P70)
* lambda 表达式
    * > 核心定义：使用 `lambda` 关键字定义的单行轻量级无名函数。
    * 语法格式: `lambda [arg1, arg2, ...]: 单行表达式`
    * 语法约束:
        * 仅支持单行表达式，表达式的计算结果自动作为返回值，**严禁包含 return 关键字**
        * 不能包含循环、复杂分支赋值，仅用于临时一次性计算逻辑（如传递给 `sorted(key=...)`）

---

## 2. 核心传参方式与参数语法矩阵

| 传参方式 | 声明与调用形态 | 接收容器形式 | 核心约束与注意点 |
| :--- | :--- | :--- | :--- |
| **位置参数** | `def f(a, b):` -> `f(1, 2)` | 独立命名变量 | 数量必须严格一致，按位置顺序绑定 |
| **关键字参数** | `f(b=2, a=1)` | 独立命名变量 | 位置参数必须在关键字参数左侧 |
| **缺省参数** | `def f(a, b=10):` | 默认值兜底 | 必须靠右声明；慎用可变对象作为默认值 |
| **位置不定长** | `def f(*args):` | `tuple` (元组) | 吸收所有多余位置参数，单星号声明 |
| **关键字不定长** | `def f(**kwargs):` | `dict` (字典) | 吸收所有键值对参数，双星号声明 |
| **lambda 表达式** | `lambda x, y: x + y` | 匿名函数对象 | 仅限单行表达式，隐式 return |

---

## 3. 核心机制对比横评

### 对比一：`*args` vs `**kwargs`
| 维度 | `*args` | `**kwargs` |
| :--- | :--- | :--- |
| **符号定义** | 单星号 `*` | 双星号 `**` |
| **传入实参形态** | 纯值序列（如 `1, 2, "a"`） | 键值对或关键字命名（如 `name="Tom", age=18`） |
| **内部封装容器** | 元组 `tuple` | 字典 `dict` |
| **解包逆向操作** | `func(*list_obj)` 展开列表为位置参数 | `func(**dict_obj)` 展开字典为关键字参数 |

### 对比二：标准 `def` 函数 vs `lambda` 匿名函数
| 维度 | `def` 标准函数 | `lambda` 匿名函数 |
| :--- | :--- | :--- |
| **代码组织** | 多行完整代码块 | 严格单行表达式 |
| **内部语句** | 支持循环、赋值、复杂异常处理 | 严禁多语句，仅支持单表达式计算 |
| **返回值处理** | 显式 `return` 决定输出 | 表达式求值结果自动隐式作为输出 |
| **复用场景** | 核心业务逻辑模块化封装，多处调用 | 临时传递给高阶函数的微型比较/映射逻辑 |
"""
    (notes_dir / "模块04_函数基础与进阶_复习笔记.md").write_text(mod04_content.strip() + "\n", encoding="utf-8")
    print("[✓] 已生成 CS-Xmind-Note 风格笔记: 模块04_函数基础与进阶_复习笔记.md")

    # -------------------------------------------------------------
    # 模块 05：类型注解与模块化编程 (P71 ~ P76)
    # -------------------------------------------------------------
    mod05_content = """# 模块 05：类型注解与模块化编程（涵盖 P71 - P76）

> 工业级静态防御与工程工程化：类型提示系统、模块隔离与包结构 | 风格：精简 (minimal) - CS-Xmind-Note 408考研思维导图树状笔记 | 涵盖分集：P71 ~ P76 | 核心议题：PEP 526 变量注解、PEP 484 函数注解、Union联合类型、import模块导入机制、__all__ 符号导出控制、__name__ 主程序守卫、package包与pip生态

---

## 知识拓扑框架导图

```text
模块 05：类型注解与模块化编程 (Type Hinting & Modular Architecture)
├── [1] Python 静态类型注解体系 (PEP 484 & 526) (P71 - P73)
│   ├── 基础与容器类型注解 (P71) ─ 显式声明类型 (var: int)、泛型容器注解 list[str]
│   ├── 函数签名类型注解 (P72) ── 参数与返回值显式约束 (def f(a: int) -> str:)
│   └── 联合类型与高级注解 (P73) ─ Union 多类型联合与 Optional 空值兼容
└── [2] 模块化工程与第三方生态 (P74 - P76)
    ├── 模块导入语法体系 (P74) ── import / from...import / as 别名机制
    ├── 自定义模块安全规范 (P75) ─ if __name__ == '__main__' 守卫与 __all__ 符号表
    └── 包 (Package) 与 pip 生态 (P76) __init__.py 标记作用与国内镜像源加速
```

---

## 1. 核心知识思维导图与考纲笔记

### 1.1 变量类型注解基础与容器泛型 (P71)
* 类型注解 (Type Hints)
    * > 核心定义：在代码中显式标注变量、参数或返回值的期望数据类型，用于辅助 IDE 静态类型检查与代码补全。
    * 核心原则: **仅作类型提示，不影响运行时动态执行**（即使传入不匹配的类型，Python 解释器运行时也不会报错抛出异常）。
    * 变量声明语法
        * 基础数据类型: `var: int = 10; is_valid: bool = True`
        * 复合容器类型: `my_list: list[int] = [1, 2, 3]; my_dict: dict[str, int] = {"age": 18}`
    * 注释中添加注解:
        * 语法: `var = 10  # type: int`（适用于无法直接在变量后加冒号的遗留环境）

### 1.2 函数形参与返回值类型注解 (P72)
* 函数类型注解
    * > 核心定义：在函数定义签名中，显式声明各个形参的类型以及最终返回值的类型。
    * 语法格式:
        * `def add(x: int, y: int) -> int:\n    return x + y`
    * 价值:
        * 调用该函数时，IDE 能准确识别输入参数类型，智能提示补全对应类型的合法方法，大幅降低工程笔误

### 1.3 联合类型 Union 与高级类型约束 (P73)
* 联合类型 (Union)
    * > 核心定义：声明某个变量或参数可以接受多种互斥的类型之一。
    * 语法规范
        * `from typing import Union`
        * 容器联合: `my_list: list[Union[str, int]] = [1, "hello", 2]`
        * 函数联合: `def process(data: Union[int, float]) -> Union[str, None]: ...`
    * Python 3.10+ 新语法:
        * 管道符写法: `int | float` 等价于 `Union[int, float]`，无需显式 import typing

### 1.4 模块概念与导入机制 (P74)
* 模块 (Module)
    * > 核心定义：一个以 `.py` 结尾的 Python 源代码文件，是函数、类、变量和可执行代码的逻辑封装单元。
    * 五种标准导入语法
        * `import 模块名`: 导入整个模块，调用格式为 `模块名.功能名()`
        * `from 模块名 import 功能名`: 仅导入指定函数或类，可直接使用 `功能名()`
        * `from 模块名 import *`: 导入该模块的所有公开符号（易导致命名污染，工业界严禁滥用）
        * `as` 别名定义: `import 模块名 as 别名` 或 `from 模块名 import 功能 as 别名`（解决名称冲突）

### 1.5 自定义模块与 __name__ / __all__ 规范 (P75)
* 自定义模块工程规范
    * > 核心定义：开发者自行编写的 Python 文件被其他脚本作为模块导入时的隔离机制。
    * `if __name__ == '__main__':` 测试守卫
        * 底层原理: 任何 `.py` 文件作为程序主入口直接运行时，内置变量 `__name__` 恒等于 `'__main__'`；被其他脚本作为模块 `import` 时，`__name__` 等于该模块文件名
        * 核心作用: 防止模块内部的调试/测试代码在被其他文件导入时自动执行
    * `__all__` 符号导出列表
        * 语法: `__all__ = ['func_a', 'ClassB']`
        * 行为: 仅对 `from 模块 import *` 生效，限制仅能导入 `__all__` 显式列出的符号，其余未列出的函数保持私有隐藏

### 1.6 Python 包结构与第三方库 pip 管理 (P76)
* 包 (Package) 与 pip 生态
    * > 核心定义：包是包含 `__init__.py` 文件的物理目录，用于在文件系统层级管理多个关联的模块文件。
    * `__init__.py` 的作用
        * 目录标识: 标记该文件夹为一个正规的 Python 包
        * 初始化执行: 导入包或其子模块时，`__init__.py` 会自动首先执行，通常用于预加载公共组件或定义包级 `__all__`
    * 第三方包生态 (PyPI & pip)
        * 安装命令: `pip install 包名`
        * 国内加速镜像源配置: `pip install 包名 -i https://pypi.tuna.tsinghua.edu.cn/simple`

---

## 2. 关键语法与模块化命令矩阵

| 语法/命令 | 代码书写范式 | 核心行为 / 作用域 | 工业级推荐程度 |
| :--- | :--- | :--- | :--- |
| **基础注解** | `x: int = 10` | 静态提示，不影响运行期动态计算 | ★★★★★ (强制规范) |
| **函数注解** | `def f(x: int) -> str:` | 明确函数输入输出契约与 IDE 补全 | ★★★★★ (强烈推荐) |
| **联合注解** | `x: Union[int, str]` / `int \| str` | 支持混合类型，增强接口弹性 | ★★★★☆ |
| **命名空间导入** | `import math` -> `math.sqrt()` | 命名空间完整，绝无命名冲突 | ★★★★★ (最安全) |
| **定向导入** | `from time import sleep` | 简化书写，直接调用 `sleep()` | ★★★★☆ |
| **主程序守卫** | `if __name__ == '__main__':` | 区分直接运行与被导入运行场景 | ★★★★★ (工业必加) |
| **导出控制** | `__all__ = ['a', 'b']` | 限制 `import *` 的符号暴露范围 | ★★★★☆ |

---

## 3. 核心机制对比横评

### 对比一：普通文件夹 vs Python 包 (Package)
| 判定维度 | 普通文件目录 | Python 包 (Package) |
| :--- | :--- | :--- |
| **物理文件标志** | 无特殊文件要求 | 必须包含 `__init__.py`（Python 3.3+ 命名空间包例外，但工业界仍强制要求） |
| **导入语法支持** | 无法直接被 Python 代码当作模块包导入 | 支持 `import package.submodule` 级联导入 |
| **执行初始化** | 无任何自动执行逻辑 | 导入该包时会自动先触发执行 `__init__.py` |

### 对比二：`import 模块` vs `from 模块 import *`
| 维度 | `import 模块名` | `from 模块名 import *` |
| :--- | :--- | :--- |
| **命名空间污染** | **零污染**：所有功能挂载在模块名命名空间下 | **严重污染**：直接将所有公开符号灌入当前局部全局作用域 |
| **重名覆盖风险** | 无风险，不同模块的同名函数可共存 | 极高风险，后导入的同名函数会静默覆盖先定义的函数 |
| **代码可读性** | 极佳，一眼能看出某个函数来自哪个模块 | 差，无法回溯某个函数到底从哪个模块导入 |
"""
    (notes_dir / "模块05_类型注解与模块化编程_复习笔记.md").write_text(mod05_content.strip() + "\n", encoding="utf-8")
    print("[✓] 已生成 CS-Xmind-Note 风格笔记: 模块05_类型注解与模块化编程_复习笔记.md")

    # -------------------------------------------------------------
    # 模块 06：面向对象基础 (P77 ~ P85)
    # -------------------------------------------------------------
    mod06_content = """# 模块 06：面向对象基础（涵盖 P77 - P85）

> 现实世界软件建模抽象：类与对象、封装继承与魔法方法 | 风格：精简 (minimal) - CS-Xmind-Note 408考研思维导图树状笔记 | 涵盖分集：P77 ~ P85 | 核心议题：OOP设计思想、class定义与实例化、self指针机制、__init__构造函数、常用魔法方法、封装与私有成员、继承与多继承MRO、方法重写与super()

---

## 知识拓扑框架导图

```text
模块 06：面向对象基础 (Object-Oriented Programming Fundamentals)
├── [1] 类与对象核心认知 (P77 - P80)
│   ├── OOP 设计范式 (P77) ────── 面向过程 (POP) vs 面向对象 (OOP)
│   ├── class 定义与实例化 (P78)  类模板抽象与基于模板实例化物理对象
│   ├── 成员变量与成员方法 (P79)  状态属性与行为能力封装
│   └── self 指针机制 (P80) ──── 实例方法首参数绑定当前调用对象的物理地址
├── [2] 内置魔法方法体系 (P81 - P82)
│   ├── 构造方法 __init__() (P81) 对象内存开辟后的属性动态初始化
│   └── 常用核心魔法方法 (P82) ── __str__ 字符串化 / __lt__ / __le__ / __eq__
└── [3] 面向对象核心特性 (P83 - P85)
    ├── 封装特性与私有成员 (P83) ─ 双下划线 __var / __func() 隐藏与只读暴露
    ├── 继承特性与多继承 (P84) ── 单继承、多继承与 C3 MRO 左侧优先搜索
    └── 方法重写与 super() (P85)  子类重写覆盖父类逻辑与 super() 协同调用
```

---

## 1. 核心知识思维导图与考纲笔记

### 1.1 面向对象思想与类与对象认知 (P77 - P78)
* 面向对象编程 (OOP)
    * > 核心定义：以“对象”为基本单元的编程范式，将数据与处理数据的函数绑定封装在一起。
    * 类 (Class) 与 对象 (Object) 关系
        * 类是抽象设计图纸（模板）: 规定了该类事物所共同拥有的属性与行为
        * 对象是具体物理实体: 根据类图纸在堆内存中实例化分配的一块具象空间

### 1.2 class 声明与 self 指针绑定 (P79 - P80)
* 类声明与 self 关键字
    * 语法格式
        * `class 类名:\n    类属性...\n    def 实例方法(self, arg1, ...):\n        self.属性 = ...`
    * `self` 的底层运行机理
        * 声明必须: 定义类中任何实例方法时，首个形参**必须保留为 self**（名称约定，可改但严禁修改）
        * 调用自动传入: 通过 `obj.method()` 调用时，Python 解释器自动将 `obj` 的堆内存地址作为第一个参数隐式传给 `self`
        * 区分作用域: 方法内通过 `self.xxx` 访问实例变量，若无 self 前缀则被视为方法局部临时变量

### 1.3 构造方法 __init__() 属性初始化 (P81)
* 构造函数 `__init__()`
    * > 核心定义：Python 类中的初始化魔法方法，在对象通过 `类名(...)` 实例化时被解释器自动触发执行。
    * 运行流程:
        * 1. 解释器在内存中开辟新对象空间
        * 2. 自动调用 `__init__(self, ...)` 并将新对象传给 self
        * 3. 在 `__init__` 内部通过 `self.name = name` 动态向对象实例上挂载属性
        * 4. 自动返回已初始化完毕的对象实例

### 1.4 常用核心魔法方法矩阵 (P82)
* 魔法方法 (Dunder Methods)
    * > 核心定义：Python 类中双下划线开头和结尾的内置特殊方法，用于定义类的内置运算与系统行为。
    * 核心高频魔法方法
        * `__init__(self, ...)`: 构造初始化函数
        * `__str__(self) -> str`: 定义对象的字符串表示形式（供 `print(obj)` 或 `str(obj)` 调用，默认打印内存地址）
        * `__lt__(self, other) -> bool`: 定义小于 `<` 运算符比较规则（同时自动支持 `>` 反向判定）
        * `__le__(self, other) -> bool`: 定义小于等于 `<=` 运算符比较规则
        * `__eq__(self, other) -> bool`: 定义等价 `==` 运算符比较规则（默认比对内存地址）

### 1.5 封装特性与私有成员机制 (P83)
* 封装 (Encapsulation)
    * > 核心定义：将对象的属性和实现细节隐藏在内部，仅对外提供公共访问接口。
    * 私有成员定义规则
        * 私有属性: 以**双下划线 `__` 开头**的变量名（如 `self.__money = 100`）
        * 私有方法: 以**双下划线 `__` 开头**的函数名（如 `def __internal_check(self):`）
    * 访问权限控制
        * 内部可见: 类内部的其他方法可以通过 `self.__money` 正常访问调用
        * 外部屏蔽: 类的实例对象在外部直接调用 `obj.__money` 会抛出 `AttributeError`

### 1.6 继承特性与多继承 MRO 搜索 (P84)
* 继承 (Inheritance)
    * > 核心定义：子类（派生类）自动获得父类（基类）中所有公开属性与方法的机制，实现代码复用。
    * 单继承 vs 多继承
        * 单继承: `class Child(Parent):`
        * 多继承: `class Child(Base1, Base2, Base3):`
    * 多继承同名冲突解决 (C3 算法与 MRO)
        * 优先级规则: 多继承中若多个父类拥有同名方法，**按括号内从左到右的次序优先继承生效**（左侧优先级高于右侧）
        * 检查工具: 通过 `类名.mro()` 或 `类名.__mro__` 可查看其完整的方法解析顺序链条

### 1.7 方法重写与 super() 协同调用 (P85)
* 方法重写 (Method Overriding)
    * > 核心定义：子类定义与父类同名同参数的方法，以覆盖父类的原有实现。
    * 调用父类原有逻辑的两种方案
        * 传统父类名直调: `父类名.方法(self, ...)`（需显式传入 self，多继承下维护成本高）
        * 推荐 `super()` 动态调用: `super().方法(...)`
            * 特点: 无需手动传 self，自动基于 MRO 链条寻找下一个层级的父类实现，完美支持菱形继承

---

## 2. 常用魔法方法与行为矩阵

| 魔法方法签名 | 触发条件 / 操作符 | 默认行为 | 重写后的典型价值 |
| :--- | :--- | :--- | :--- |
| `__init__(self, ...)` | `obj = ClassName(...)` 实例化 | 空操作 | 接收外部参数，为对象初始化私有属性 |
| `__str__(self)` | `print(obj)` 或 `str(obj)` | 打印 `<__main__.Class at 0x...>` | 返回友好的结构化文本信息，方便调试 |
| `__lt__(self, other)` | `obj1 < obj2` | 抛出 TypeError（不支持比较） | 基于自定义属性比较对象大小，支持 `sorted()` |
| `__le__(self, other)` | `obj1 <= obj2` | 抛出 TypeError | 定义小于等于比较规约 |
| `__eq__(self, other)` | `obj1 == obj2` | 比对两者物理内存地址是否相同 | 比对两个对象的业务字段（如 ID 或数值） |

---

## 3. 面向对象核心机制对比

### 对比一：面向过程 (POP) vs 面向对象 (OOP)
| 维度 | 面向过程 (POP - Procedure Oriented) | 面向对象 (OOP - Object Oriented) |
| :--- | :--- | :--- |
| **思维视角** | 以“步骤”为中心（先做A，再做B，后做C） | 以“实体对象”为中心（谁拥有什么数据，谁能做什么） |
| **代码单元** | 函数、模块、全局变量 | 类、对象、实例方法、属性封装 |
| **复杂度应对** | 适合小型脚本、简单数据处理流程 | 适合大型复杂软件架构、业务实体丰富系统 |
| **可维护性** | 需求变更时易牵一发而动全身 | 模块高内聚低耦合，利于扩展和维护 |

### 对比二：单继承 vs 多继承
| 维度 | 单继承 | 多继承 |
| :--- | :--- | :--- |
| **继承声明** | `class Sub(Base):` | `class Sub(B1, B2):` |
| **层次关系** | 单一线性树状结构 | 网状复式结构，易出现菱形继承 |
| **冲突处理** | 无冲突，子类覆盖父类即生效 | 存在同名冲突，依赖 MRO 左侧优先规则裁决 |
"""
    (notes_dir / "模块06_面向对象基础_复习笔记.md").write_text(mod06_content.strip() + "\n", encoding="utf-8")
    print("[✓] 已生成 CS-Xmind-Note 风格笔记: 模块06_面向对象基础_复习笔记.md")

    # -------------------------------------------------------------
    # 模块 07：异常处理与容错机制 (P86 ~ P87)
    # -------------------------------------------------------------
    mod07_content = """# 模块 07：异常处理与容错机制（涵盖 P86 - P87）

> 程序健壮性与生产级防御：Traceback 溯源、多级捕获与资源保障 | 风格：精简 (minimal) - CS-Xmind-Note 408考研思维导图树状笔记 | 涵盖分集：P86 ~ P87 | 核心议题：异常 (Exception) 概念、try...except 结构、特定异常捕获、else分支、finally资源必释放、主动 raise 抛出

---

## 知识拓扑框架导图

```text
模块 07：异常处理与容错机制 (Exception Handling & Fault Tolerance)
├── [1] 异常认知与基础捕获 (P86)
│   ├── 异常本质与 Traceback ──── 运行时错误信号与调用栈追溯链
│   └── try...except 基础语法 ── 捕获错误、阻止程序崩溃退出
└── [2] 工业级异常处理完整体系 (P87)
    ├── 捕获特定异常类型 ─────── 精准捕获 IndexError / ZeroDivisionError 等
    ├── 捕获多个异常元组 ─────── except (TypeError, ValueError) as e:
    ├── 捕获全局兜底异常 ─────── except Exception as e:
    ├── else 分支逻辑 ────────── 无异常发生时的确定性操作
    └── finally 资源保底释放 ─── 无论是否崩溃必执行（IO/连接安全释放）
```

---

## 1. 核心知识思维导图与考纲笔记

### 1.1 异常认知与基础捕获拦截 (P86)
* 异常 (Exception)
    * > 核心定义：程序在运行过程中发生的错误信号，若未被捕获拦截，将导致程序立即崩溃中断并打印调用栈（Traceback）。
    * 基础捕获拦截语法
        * 结构: `try:\n    可能触发异常的代码\nexcept:\n    异常发生时的兜底捕获代码`
    * 运行机理
        * 正常流程: `try` 内代码无异常执行完毕，直接跳过 `except` 块
        * 异常流程: `try` 内某行代码抛出错误，该行之后的代码立即中断执行，控制权瞬间移交给 `except` 块

### 1.2 工业级异常精准捕获完整结构 (P87)
* 完整异常处理四段式结构 (try-except-else-finally)
    * 语法格式
        * `try:`（监控可能发生错误的代码块）
        * `except <ExceptionType> as e:`（精准捕获匹配的异常对象）
        * `else:`（可选，**仅当 try 块没有触发任何异常时**才执行）
        * `finally:`（可选，**无论是否发生异常、无论是否被捕获，均必然执行**）
    * 捕获策略分类
        * 1. 捕获指定单一异常: `except ZeroDivisionError as e:`（仅拦截除零错误）
        * 2. 捕获指定多个异常: `except (IndexError, KeyError) as e:`（元组形式组合拦截）
        * 3. 捕获所有通用异常: `except Exception as e:`（Exception 是绝大多数内置非系统退出异常的基类）
    * `finally` 的工程价值
        * 资源安全保障: 用于文件句柄关闭 (`file.close()`)、数据库连接释放、网络 Socket 释放或互斥锁解开，杜绝系统资源泄漏

### 1.3 异常的传递性与主动抛出 (P87 延伸)
* 异常传递性 (Exception Propagation)
    * 当函数内部发生异常且未在当前层级被捕获时，异常会沿调用栈依次向外层调用者传递，若最顶层主程序仍未捕获，最终由解释器终止进程并输出 Traceback。
* 主动抛出异常: `raise`
    * 语法: `raise ValueError("非法参数输入")`
    * 适用场景: 业务前置条件校验不满足时，主动抛出业务异常阻断后续错误操作。

---

## 2. 核心异常处理语法结构矩阵

| 语法关键字 | 触发条件 / 执行时序 | 核心工程价值 | 推荐使用场景 |
| :--- | :--- | :--- | :--- |
| **try** | 必选，监控代码段 | 标记风险操作范围 | 涉及网络 I/O、文件读写、用户动态输入的易错操作 |
| **except** | try 内抛出对应类型异常时执行 | 捕获错误、记录日志、降级响应 | 阻止程序崩溃，提供异常诊断与兜底方案 |
| **else** | try 内**未发生任何异常**时执行 | 与风险代码隔离，确保正常流 | 确保数据验证通过后再执行后续持久化操作 |
| **finally** | **无论如何必然执行**（即便遇到 return） | 绝对保证资源释放与现场清理 | 文件流关闭、数据库事务连接池归还、释放锁 |

---

## 3. 常见内置异常排查与对比

| 内置异常名称 | 触发常见根因 | 典型代码防范范式 |
| :--- | :--- | :--- |
| **IndexError** | 序列下标超出合法范围 | `if 0 <= idx < len(lst):` 或使用切片 |
| **KeyError** | 访问字典中不存在的键 | 使用 `dict.get(key, default)` 替代直接下标访问 |
| **TypeError** | 数据类型不匹配或参数个数不匹配 | `isinstance()` 前置验证或类型注解规范 |
| **ValueError** | 传入参数类型正确但取值非法（如 `int("abc")`） | 结合 `try...except ValueError` 拦截非法输入 |
| **ZeroDivisionError** | 除法或取模运算中除数为 0 | `if divisor != 0:` 前置防御 |
| **FileNotFoundError** | 打开不存在的文件路径 | `from pathlib import Path; if p.exists():` |
"""
    (notes_dir / "模块07_异常处理与容错机制_复习笔记.md").write_text(mod07_content.strip() + "\n", encoding="utf-8")
    print("[✓] 已生成 CS-Xmind-Note 风格笔记: 模块07_异常处理与容错机制_复习笔记.md")

if __name__ == "__main__":
    import sys
    target = Path("output/黑马程序员Python+AI零基础入门到大神全套视频课程，覆盖Python核心语法、AI应用、数据分析及Web应用等python实战项目开发全流程_BV1sHU")
    if not target.exists():
        print(f"Error: {target} does not exist", file=sys.stderr)
        sys.exit(1)
    generate_notes(target)
